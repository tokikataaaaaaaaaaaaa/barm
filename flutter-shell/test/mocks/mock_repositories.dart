// Repository interfaces will be defined here once domain layer is implemented
// For now, create placeholder mocks

// Mock for AuthRepository
// Usage:
// final mockAuth = MockAuthRepository();
// when(() => mockAuth.signInWithApple()).thenAnswer((_) async => testUser);
// class MockAuthRepository extends Mock implements AuthRepository {}

// Mock for MissionRepository
// class MockMissionRepository extends Mock implements MissionRepository {}

// Mock for RecordRepository
// class MockRecordRepository extends Mock implements RecordRepository {}

// Mock for GroupRepository
// class MockGroupRepository extends Mock implements GroupRepository {}

// Mock for SubscriptionRepository
// class MockSubscriptionRepository extends Mock implements SubscriptionRepository {}

// Placeholder to make the file valid Dart
class MockPlaceholder {}
